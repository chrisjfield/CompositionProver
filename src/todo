Lookup methods
How to Use tab

composition page
default stages

bobs with multiple notations
repeating notations (with comma) (split on comma, keep le change, pop and push)

allow method splicing

results page show:
* row count
* change of method count
* lead ends
* course ends
* musical changes count
* grid

publish and check mobile (tabs?)

save setups